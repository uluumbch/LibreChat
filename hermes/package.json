{
  "name": "hermes-chat",
  "private": true,
  "version": "0.1.0",
  "description": "Hermes-native multi-user chat application (uses LibreChat as a reference). Hermes Agent (Nous Research) is the only backend engine.",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspace shared --if-present && npm run typecheck --workspace server --if-present && npm run typecheck --workspace client --if-present",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server"
  },
  "engines": {
    "node": ">=20"
  }
}
