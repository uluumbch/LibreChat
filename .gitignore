node_modules/
dist/
build/
*.log
.env
.env.local
.DS_Store
coverage/
# Prisma generated client
server/src/generated/

# The repo-root .gitignore has broad rules (`data/`, `*.d.ts`) that also match
# Hermes-Chat *source*. Re-include it so it gets committed (build output and the
# Prisma client stay ignored via dist/ and server/src/generated/ above).
!client/src/data/
!client/src/data/**
!**/*.d.ts
